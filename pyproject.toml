[project]
name = "ecaa-book-root"
version = "0.1.0"
requires-python = ">=3.13"

[tool.uv.workspace]
members = [
    # tools
    "mdocx",
    "chroma-repl",
    # project
    "common",
    "chapter02",
    "chapter03",
    "chapter04",
    # mcp servers
    "mcp_servers/experiential_memory",
    # Add more chapters as needed
]

[tool.uv.sources]
common = { workspace = true }
